name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '4.0.0'
      SERVERPOD_PASSWORD_database: 'jXqUaFUT1F0URlaqIfBKRMQ04mrqr6Es'
      SERVERPOD_PASSWORD_redis: 'EltuS1m7SLwCfHS8EgsTk82BZFhQwiLE'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.4'

      - name: Start containers
        working-directory: stream_studio_server
        run: docker compose up --quiet-pull --build -d

      - name: Install dependencies
        run: flutter pub get

      - name: Install Serverpod CLI
        run: |
          dart pub global activate serverpod_cli $VERSION
          echo "$HOME/.pub-cache/bin" >> $GITHUB_PATH
          echo "$HOME/.local/state/Dart/install/bin" >> $GITHUB_PATH

      - name: Run Serverpod generator
        working-directory: stream_studio_server
        run: serverpod generate

      - name: Run tests
        working-directory: stream_studio_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: stream_studio_server
        run: docker compose down -v
